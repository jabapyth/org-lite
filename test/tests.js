
var expect = require('chai').expect
  , fs = require('fs')
  , path = require('path')
  , wrench = require('wrench')

  , base = '../lib' + (process.env.EXPRESS_COV ? '-cov' : '')
  , OrgFile = require(base);

describe('OrgFile', function () {
});

